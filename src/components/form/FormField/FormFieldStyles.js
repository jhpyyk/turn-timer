import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: 300,
    height: 50,
    margin: 10,
    padding: 10,
    color: "white",
    borderWidth: 1,
    borderColor: "white",
  },
});

export default styles;
